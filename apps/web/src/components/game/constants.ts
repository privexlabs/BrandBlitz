// Values must match packages/stellar/src/constants.ts.
// Parity is enforced by constants.test.ts.
export const WARMUP_MIN_SECONDS = 20;
export const ROUND_SECONDS = 15;
export const TOTAL_ROUNDS = 3;
